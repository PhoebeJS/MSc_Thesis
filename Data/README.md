# Data

---

The excel sheets linked here are the resulting metrics calculated & recorded per run. The excel (here) [] are the main results per model. Different sheets in the excel relate to the different datasets as well as to results recorded during the information extraction task. 

The second (excel sheet) {} relates to the metrics calculated for records classified as 'mixed' or 'table' within the BL dataset.
